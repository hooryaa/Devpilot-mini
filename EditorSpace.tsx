// src/components/figma-ui/dashboard/EditorSpace.tsx
import React from "react";

/**
 * Minimal stub so esbuild stops failing.
 * Option A does NOT use a full EditorSpace, so this safely does nothing.
 */
export default function EditorSpace() {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        background: "transparent",
      }}
    />
  );
}
